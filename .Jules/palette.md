# Critical UX/a11y Lessons

- Empty states in lists must use a centered ghost icon (SVG with `aria-hidden="true"`), concise headline, and CTA.
- Status indicators must use Tailwind `dark:` modifiers instead of hardcoded hex colors for dark mode contrast.
