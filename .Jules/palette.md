Critical UX/a11y Lesson: Empty states in the UI should never be plain text. Always provide a centered ghost icon (SVG) with `aria-hidden="true"` for accessibility, a concise headline explaining the empty state, and an actionable CTA button or link guiding the user on what to do next. Ensure interactive elements like links and buttons have proper keyboard focus indicators like `focus-visible:outline-primary`.
